# Students will be on Windows, macOS and Linux. Normalise to LF so the
# shell scripts stay runnable everywhere.
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf

*.png binary
*.jpg binary
*.ico binary
*.ttf binary
*.otf binary
