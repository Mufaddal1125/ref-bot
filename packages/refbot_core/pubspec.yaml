name: refbot_core
description: Rules shared by the RefBot client and its tests, with no Flutter dependency.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
