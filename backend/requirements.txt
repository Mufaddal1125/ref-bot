# Pinned so every machine in the room resolves the same tree.

Django==6.1
djangorestframework==3.18.0
django-cors-headers==4.9.0
django-extensions==4.1

# Phase 2 — WebSockets
channels==4.3.2
channels_redis==4.3.0
daphne==4.2.3

# Phase 3 — cache and job queue
django-redis==7.0.0
django-rq==4.1.1

psycopg[binary]==3.3.4
dj-database-url==3.1.2
python-dotenv==1.2.3

# Phase 3 — AI referee
httpx==0.28.1
pydantic==2.13.5

pytest==9.1.1
pytest-django==4.14.0
